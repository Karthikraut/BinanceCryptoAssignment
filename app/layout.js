import React from 'react'

const layout = ({children}) => {
  return (
    <html>
        <head>
        </head>
        <body>
            {children}
        </body>
    </html>
  )
}

export default layout